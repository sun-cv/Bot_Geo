const { identifierEmojis } = require('./constants/identifierEmoji');
const { mercyConditions } = require('./constants/mercyConditions');
const { shardColor } = require('./constants/shardColor');
const { shardIdentifiers } = require('./constants/shardIdentifiers');


module.exports = {
	identifierEmojis,
	mercyConditions,
	shardColor,
	shardIdentifiers,
};